import { Typography } from "@mui/material"

export const CheckoutPage = (): React.ReactNode => {
    return (
        <>
        <Typography variant="h3">
            stuff for only logged in users
        </Typography>
        </>
    )
}